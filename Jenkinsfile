pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application'
            }
        }
    
        stage('Test') {
            steps {
                echo 'Running the tests'
            }
        }
        
        stage('Deployin') {
            steps {
                echo 'Deployment of application'
            }
        }
        
    }
}
